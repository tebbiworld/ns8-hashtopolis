#!/usr/bin/env python3

#
# Copyright (C) 2026 tebbi
# SPDX-License-Identifier: GPL-3.0-or-later
#

import os
import sys
import json

import agent

config = {}

config["host"] = os.getenv("TRAEFIK_HOST", "")
config["http2https"] = os.getenv("TRAEFIK_HTTP2HTTPS") == "True"

route = agent.get_route(os.environ["MODULE_ID"])
config["lets_encrypt"] = route.get("lets_encrypt", False)

# Public base URL override (empty = derived from host + Traefik settings).
config["public_url"] = os.getenv("PUBLIC_URL", "")

config["backend_url"] = os.getenv("BACKEND_URL", "")

# URL a v1 agent has to be registered with (python agent: --url).
base_url = os.getenv("BASE_URL", "")
config["agent_url"] = f"{base_url}/api/server.php" if base_url else ""

# Initial admin credentials seeded on first start (valid until changed in the
# Hashtopolis UI).
config["admin_username"] = os.getenv("ADMIN_USER", "admin")
config["admin_password"] = os.getenv("ADMIN_PASSWORD", "")

json.dump(config, fp=sys.stdout)
